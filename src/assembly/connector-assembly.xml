<!--
  Assembly descriptor for the connector ZIP packaging.
  Produces a ZIP containing:
    *.def, *.impl, *.properties, *.png  - at root
    classpath/                          - SDK dependency JARs + project JAR

  NOTE: scope is "provided" because all API client dependencies use provided scope
  to prevent Bonita Studio import failures (the Studio's analyze plugin rejects
  unresolvable compile/runtime deps). The assembly still bundles them here for
  production deployment, and the dependencies-as-var.groovy script lists them
  in <jarDependencies> inside the .impl so the Bonita runtime loads them.
-->
<assembly
        xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2 http://maven.apache.org/xsd/assembly-1.1.2.xsd">
    <id>all</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <fileSet>
            <outputDirectory/>
            <directory>target/classes</directory>
            <includes>
                <include>*.*</include>
            </includes>
        </fileSet>
        <fileSet>
            <outputDirectory>classpath</outputDirectory>
            <directory>${basedir}/target</directory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>
    </fileSets>
    <dependencySets>
        <dependencySet>
            <unpack>false</unpack>
            <scope>provided</scope>
            <outputDirectory>classpath</outputDirectory>
        </dependencySet>
    </dependencySets>
</assembly>
